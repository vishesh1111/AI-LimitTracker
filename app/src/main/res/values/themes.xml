<resources>
    <!-- Base theme — Activity uses Compose, so this is minimal -->
    <style name="Theme.ClaudeTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
